describe.skip('Test if the Chatbot can handle errors and invalid inputs gracefully', async () => {
  before('Launch the browser and load the web url', async () => {});

  it('1. Chat bot should provide appropriate error message when gets invalid input from user', async () => {});

  it('2. Chat bot should recognize special characters', async () => {});

  it('3. Chat bot text box should be disabled if user is entering audio message', async () => {});

  after('Close the browser', async () => {});
});
